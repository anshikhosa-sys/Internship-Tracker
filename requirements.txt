# Runtime. Every dependency is free and runs locally; nothing here can call a
# paid API (tests/test_llm.py asserts it).
httpx>=0.27          # source fetching and the local Ollama backend
requests>=2.32       # legacy source fetchers (replaced by httpx in the async pipeline)
Flask>=3.0           # the dashboard
pypdf>=5.0           # PDF résumés (pure Python)
numpy>=1.26          # vector math for semantic matching
fastembed>=0.4       # local ONNX embedding model; optional — hashed vectors are the fallback

# Development
pytest>=8.0
playwright>=1.40     # browser_tests.py: python3 -m playwright install chromium
